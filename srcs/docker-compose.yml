services:
  mariadb:
    build: ./requirements/mariadb
    container_name: mariadb
    restart: always
    env_file:
      - .env
    volumes:
      - mariadb_data:/var/lib/mysql
    networks:
      - inception

  wordpress:
    build: ./requirements/wordpress
    container_name: wordpress
    restart: always
    env_file:
      - .env
    volumes:
      - wordpress_data:/var/www/html
    depends_on:
      - mariadb
    networks:
      - inception

  nginx:
    build: ./requirements/nginx
    container_name: nginx
    restart: always
    ports:
      - "443:443"
    volumes:
      - wordpress_data:/var/www/html
    depends_on:
      - wordpress
    networks:
      - inception

volumes:
  mariadb_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/akajjou/data/mariadb

  wordpress_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/akajjou/data/wordpress

networks:
  inception:
    driver: bridge


# version: "3.8"

# services:
#   mariadb:
#     image: mariadb:11
#     container_name: mariadb
#     restart: unless-stopped
#     environment:
#       MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
#       MYSQL_DATABASE: ${MYSQL_DATABASE}
#       MYSQL_USER: ${MYSQL_USER}
#       MYSQL_PASSWORD: ${MYSQL_PASSWORD}
#     volumes:
#       - mariadb_data:/var/lib/mysql

#   wordpress:
#     image: wordpress-test          # your custom image
#     container_name: wordpress
#     restart: on-failure
#     env_file:
#       - .env                        # same .env with WORDPRESS_DB_* and MYSQL_*
#     depends_on:
#       - mariadb
#     expose:
#       - "9000"                      # php-fpm port

# volumes:
#   mariadb_data:
